[build]
builder = "nixpacks"
buildCommand = "pip install -r Backend_Screenshot/requirements.txt"

[deploy]
startCommand = "cd Backend_Screenshot && uvicorn main:app --host 0.0.0.0 --port 8080"
restartPolicyType = "on_failure"
